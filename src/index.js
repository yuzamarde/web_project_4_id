// import "./styles/index.css";
// import stepsSrc from "./images/steps.png";
import {
    userProfile,
    portfolioFormHandler,
    imagePopup,
    addCardToContainerFn,
  } from './pages/index.js'; 
  
//   userProfile.doSomething(); // Example method of the userProfile component
//   imagePopup.showImage('image-url.jpg'); // Example method of the imagePopup component
  
//   // You can also use the addCardToContainerFn function to add cards to the container:
//   const cardData = { title: 'New Card', imageUrl: 'card-image.jpg' };
//   addCardToContainerFn(cardData);

// const numbers = [2, 3, 5];
// const doubledNumbers = numbers.map(number => number * 2);

// console.log(doubledNumbers); // 4, 6, 10

// const stepsImage = document.getElementById("image-steps");
// stepsImage.src = stepsSrc;
